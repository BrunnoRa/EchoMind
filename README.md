# EchoMind API 🧠

API desenvolvida com FastAPI, PostgreSQL e Docker.

### 🛠 Como Rodar o Projeto
1. **Configurar Ambiente:** Copie o arquivo `.env.example` para `.env` e preencha as credenciais.
2. **Subir com Docker:**
   ```bash
   docker compose up --build
